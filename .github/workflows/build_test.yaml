name: All Unit Test

on:
  pull_request:
    branches: 
      - '*'
  push:
    branches: [ 'feature/integrate-github' ] # only for test.
    
    
    
jobs:
  UnitTest:
    runs-on: ubuntu-24.04

    steps:
    - uses: actions/checkout@v4
      name: Checkout
        
    - name: Install Package
      shell: bash
      run: |
        sudo apt update && \
        sudo apt install -y clang-17 git cmake ninja-build \
                              curl zip unzip tar build-essential pkg-config
        
    - name: Install vcpkg
      shell: bash
      run: make vcpkg
    
    - name: Build and Test
      shell: bash
      run: make test
